<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tna_team.key_kost">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KelolaPenyewa" />
        <activity android:name=".DetailPenyewa" />
        <activity android:name=".TambahAtauUbahPenyewaKos" />
        <activity android:name=".LogActivity" android:theme="@style/AppTheme.Custom"/>
        <activity android:name=".HomeActivity" android:theme="@style/AppTheme.Custom"></activity>
    </application>

</manifest>